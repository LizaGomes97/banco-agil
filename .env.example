# ── LLM (chat) ───────────────────────────────────────────────────────────────
# Obtenha em: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=sua_chave_aqui
GEMINI_MODEL=gemini-2.0-flash
LLM_TEMPERATURE=0.3

# ── Redis (checkpointing de sessão via LangGraph) ─────────────────────────────
# Opção 1 — local: docker run -d --name redis-banco-agil -p 6379:6379 redis:alpine
# Opção 2 — VPS:   ssh -i ~/.ssh/chave.key -L 6379:127.0.0.1:6379 usuario@ip -N
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_TTL_SECONDS=1800

# ── Qdrant (memória semântica por cliente) ────────────────────────────────────
# Opção 1 — local: docker run -d --name qdrant -p 6333:6333 qdrant/qdrant
# Opção 2 — VPS:   ssh -i ~/.ssh/chave.key -L 6333:127.0.0.1:6333 usuario@ip -N
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=
QDRANT_EMBEDDING_DIMENSION=3072

# ── Embeddings (chave dedicada para não consumir cota do chat) ────────────────
# Pode ser a mesma GEMINI_API_KEY se não tiver chave separada
GEMINI_API_KEY_EMBEDDINGS=sua_chave_aqui
GEMINI_EMBEDDING_MODEL=models/gemini-embedding-001

# ── Tavily (cotação de câmbio em tempo real) ──────────────────────────────────
# Obtenha em: https://app.tavily.com
TAVILY_API_KEY=sua_chave_aqui
